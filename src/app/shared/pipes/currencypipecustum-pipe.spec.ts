import { CurrencypipecustumPipe } from './currencypipecustum-pipe';

describe('CurrencypipecustumPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencypipecustumPipe();
    expect(pipe).toBeTruthy();
  });
});
